name: Telemetry Verification

on:
  pull_request:
    paths:
      - "services/telemetry/**"
      - "scripts/verify_telemetry.sh"
      - "docker-compose.yml"
      - ".github/workflows/telemetry-verification.yml"
  workflow_dispatch:

concurrency:
  group: telemetry-verification-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify-telemetry:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and start stack
        run: docker compose up -d --build

      - name: Wait for telemetry
        run: |
          for i in {1..30}; do
            if curl -fsS http://localhost:8089/health >/dev/null; then
              exit 0
            fi
            sleep 2
          done
          echo "Telemetry service did not become healthy in time"
          docker compose logs --no-color
          exit 1

      - name: Run telemetry verification
        run: |
          chmod +x scripts/verify_telemetry.sh
          scripts/verify_telemetry.sh

      - name: Dump logs on failure
        if: failure()
        run: docker compose logs --no-color

      - name: Tear down
        if: always()
        run: docker compose down -v
